{
  "name": "meridian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "analyze:planning": "node scripts/decision-audit.mjs",
    "analyze:design": "node scripts/design-audit.mjs",
    "analyze:outcomes": "node scripts/outcome-audit.mjs",
    "build": "node scripts/build.mjs",
    "assets": "node scripts/build-forge-assets.mjs",
    "assets:preview": "node scripts/forge-preview.mjs",
    "dev": "node scripts/dev.mjs",
    "deploy": "node scripts/deploy.mjs",
    "test": "cargo test --release --lib --tests && node --test tests/*.test.mjs",
    "prepare:release": "node scripts/prepare.mjs"
  },
  "devDependencies": {
    "wrangler": "4.134.0"
  }
}
